<% if @destination %>
  $('#slideToggleLink').text("<%= init_title_destination(@destination) %>");
  $('#slideToggleLinkMobile').text("<%= init_title_destination(@destination) %>");
  $('.search_mobile span.text').text("<%= init_title_destination(@destination) %>");
<% end %>

$("#dealsHotelsList").html("<%= j render 'hotels_list' %>");

<% unless @hotels_list.first[:error_response] %>
  initDealsPage(<%= raw @hotels_list.first[:num_of_page] %>, <%= raw @hotels_list.first[:num_of_hotel] %>);
<% end %>

$('[data-toggle="tooltip"]').tooltip();

// show footer after load hotels list
$('.footer').removeClass('hide');