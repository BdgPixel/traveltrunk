p = @user.profile ? "Hi #{@user.profile.first_name}," : "Hi #{@user.email}"

p You’ve recently requested to reset your Travel Trunk password. You can do this by clicking the link below.

p If you haven’t requested to change your password, please ignore this message. Your current password will remain until you access the link and create a new one.

p = link_to 'Reset my Travel Trunk password', edit_password_url(@resource, reset_password_token: @token)

p = render 'partials/footer_mailer'
